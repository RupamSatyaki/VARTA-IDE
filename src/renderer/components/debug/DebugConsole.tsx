import React from 'react'

export function DebugConsole() {
  return (
    <div className="flex-1 flex items-center justify-center text-xs text-[#4e4e4e] italic">
      Debug Console — (no active session)
    </div>
  )
}
